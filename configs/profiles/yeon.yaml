# configs/profiles/yeon.yaml — 서버별 경로 참조 문서 (yeon)
# ⚠️ 참조 문서일 뿐, 학습 config에 자동 머지되지 않는다 (자동 머지 = 후속 과제).
# 출처: scripts/servers.conf

server:
  alias: yeon
  port: 600
  gpus_default: "0"
  notes: repo 경로가 .../Drone/... (Drone24 아님) 주의. sam2 editable 미설치 시 PYTHONPATH 필요.

repo_path: /SSDb/jemo_maeng/src/Project/Drone/detection/drone-MemorySAM
conda_env: MMSS_SAM

data_roots:
  DELIVER: FILL_ME
  MULTIAQUA: FILL_ME

pretrained:
  segformer_mit_b2: /hpc2hdd/home/cliao127/MMSS-SAM-S1/sam/checkpoints/pretrained/segformer/mit_b2.pth  # 레거시 절대경로 관행
  sam2_hiera_base_plus: semseg/models/sam2/sam2/checkpoints/sam2.1_hiera_base_plus.pt
