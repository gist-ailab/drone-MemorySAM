DEVICE          : cuda              # device used for training and evaluation (cpu, cuda, cuda0, cuda1, ...)
SAVE_DIR        : './outputs/MMSamP6/lecun_deliver_rgbd'   # ATT 버전용 별도 출력 폴더

MODEL:
  NAME          : CMNeXt                                            # name of the model you are using
  BACKBONE      : CMNeXt-B2                                         # model variant
  PRETRAINED    : '/hpc2hdd/home/cliao127/MMSS-SAM-S1/sam/checkpoints/pretrained/segformer/mit_b2.pth'     # backbone model's weight 
  RESUME_ENABLE : false                                             # enable resume training (true/false)
  RESUME_PATH   : ''                                                # checkpoint file path for resume (e.g., './outputs/.../last_checkpoint.pth')
  LORA_MODEL    : LoRA_Sam_P6                                       # LoRA model name (LoRA_Sam, LoRA_Sam_P1, LoRA_Sam_P2, LoRA_Sam_P3, LoRA_Sam_P4, LoRA_Sam_P5, LoRA_Sam_P6)
  LORA_R        : 4                                                 # LoRA rank
  LORA_NUM_EXPERTS : null                                           # number of experts for MoE (null = auto from num_modalities)
  LORA_TOP_K    : 2                                                 # top_k for MoE routing
  LORA_LAYER    : null                                                # checkpoint file 

DATASET:
  NAME          : DELIVER                                       # dataset name to be trained with (camvid, cityscapes, ade20k)
  ROOT          : '/ailab_mat2/dataset/DELIVER'                                    # dataset root path
  IGNORE_LABEL  : 255 
  MODALS        : ['img', 'depth']



  RGB_SUBROOT   : null
  THERMAL_SUBROOT : null
  LIDAR_SUBROOT : null
TRAIN:
  IMAGE_SIZE    : [1024, 1024]    # training image size in (h, w)
  BATCH_SIZE    : 2         # batch size used to train
  EPOCHS        : 200             # number of epochs to train
  EVAL_START    : 0             # evaluation interval start
  EVAL_INTERVAL : 1               # evaluation interval during training
  AMP           : false           # use AMP in training
  DDP           : True            # use DDP training

LOSS:
  NAME          : OhemCrossEntropy          # loss function name 
  CLS_WEIGHTS   : false            # use class weights in loss calculation

OPTIMIZER:
  NAME          : adamw           # optimizer name
  LR            : 0.00045         # initial learning rate used in optimizer
  WEIGHT_DECAY  : 0.01            # decay rate used in optimizer

SCHEDULER:
  NAME          : warmuppolylr    # scheduler name
  POWER         : 0.9             # scheduler power
  WARMUP        : 10              # warmup epochs used in scheduler
  WARMUP_RATIO  : 0.1             # warmup ratio
  

EVAL:
  MODEL_PATH    : '/SSDc/jemo_maeng/src/Project/Drone24/detection/drone-MemorySAM/outputs/MMSamP6/lecun_deliver_rgbd/DELIVER_CMNeXt-B2_id/epoch66_60.32_checkpoint.pth'
  IMAGE_SIZE    : [1024, 1024]                            # evaluation image size in (h, w)        
  BATCH_SIZE    : 1      
  EVAL_DAY      : false                                   # true면 test 시 RGB 서브루트를 zed_day로 사용

  MSF: 
    ENABLE      : false                                   # multi-scale and flip evaluation  
    FLIP        : true                                    # use flip in evaluation  
    SCALES      : [0.5, 0.75, 1.0, 1.25, 1.5, 1.75]       # scales used in MSF evaluation                


TEST:
  MODEL_PATH    : ''    # trained model file path
  FILE          : '/ailab_mat2/dataset/DELIVER'           # filename or foldername 
  IMAGE_SIZE    : [1024, 1024]                            # inference image size in (h, w)
  OVERLAY       : false                                   # save the overlay result (image_alpha+label_alpha)
