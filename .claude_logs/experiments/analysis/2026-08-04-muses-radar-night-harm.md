# MUSES radar 손해의 조건별 국소화 — 야간 전멸·주간 이득 (2026-08-04)

> 근거: 공식 Codabench test 두 제출의 조건별 대조. 판정 = opus 세션. 수치는 제출 결과 원문.

## 1. 대조 대상

| 제출 | 모달 | 공식 test |
|---|---|---|
| `muses_P39_1_rank_seed2` | img/lidar/event (3모달) | **79.788** ← 우리 최고 |
| `muses_P39_1_rank_4modal_seed2_ep260` | +radar (4모달) | **79.571** (−0.217) |

두 런은 **radar 추가 외 동일**(같은 base P39.1-rank, 같은 레시피, seed2). val에서도 82.62 → 82.35(−0.27)로 test 델타(−0.217)와 크기가 거의 같다.

## 2. 🔴 핵심 — 손해는 균일하지 않고 **야간에 국소화**된다

| 조건 | 3모달 | 4모달 | Δ (radar 추가) |
|---|---|---|---|
| overall | 79.788 | 79.571 | −0.217 |
| **day** | 80.246 | 80.435 | **+0.189** ✅ |
| **night** | 76.818 | 76.442 | **−0.376** |
| clear_day | 80.021 | 80.619 | **+0.598** ✅ |
| rain_day | 78.743 | 79.038 | +0.295 ✅ |
| fog_day | 76.366 | 76.240 | −0.126 |
| snow_day | 71.155 | 69.488 | −1.667 |
| clear_night | 76.059 | 73.680 | **−2.379** 🔴 |
| **fog_night** | 69.610 | 64.238 | **−5.372** 🔴🔴 |
| rain_night | 74.354 | 73.641 | −0.713 |
| snow_night | 77.413 | 75.887 | −1.526 |

- **야간 4/4 조건 전부 음수.** 주간은 혼재(2 상승·2 하락)이며 net +0.19.
- 최대 손해는 **fog_night −5.37** — radar가 물리적으로 **가장 유리해야 할** 조건이다.

⚠️ **표본 주의**: 조합 조건은 75~150장이라 단일 수치의 노이즈 폭이 크다. `−5.37` 하나를 단독 인용하지 말 것. 신뢰할 수 있는 부분은 **야간 4/4 전부 음수라는 방향성**과 day/night 부호가 갈린다는 사실이다.

## 3. 배제된 설명 — 디코딩 불일치 아님

ISSUE-025(radar를 lidar 디코더로 폴스루) 재발을 의심했으나 **아니다**:
- 학습 로더 `semseg/datasets/muses.py:_open_radar` = 픽스본(`RADAR_RANGE_MAX=150.0`, 채널 `[range/150, intensity/255, occupancy]`).
- 제출 추론은 `tools/predict_muses_test.py --radar-decode fixed` → `legacy_radar=False` → **같은 `_open_radar`**.
- 4모달 seed2 학습 시작 07-28 > ISSUE-025 픽스 07-21.
→ 학습·추론 디코딩 **일치**. 원인은 데이터 코덱이 아니라 **학습/융합** 쪽이다.

## 4. 기제 가설 — "RGB가 약해질 때 가장 정보 없는 모달로 폴백한다"

- radar는 실측상 **3번째 채널(height)이 전 픽셀 정확히 0**이라 코드가 `occupancy` 마스크로 대체했다(`muses.py:203-217` 주석). 즉 실질 정보는 range + intensity 2채널의 희소 투영뿐이며, **semantic 변별력이 구조적으로 낮다**.
- 야간에는 img의 reliability가 떨어져 P36 router가 가중치를 다른 모달로 옮긴다. 그 폴백이 **정보가 가장 적은 radar로 흘러가면** 유용한 lidar/event 증거를 밀어낸다.
- 조도가 낮을수록 손해가 커지는 관측(clear_night −2.4 → fog_night −5.4)과 정합한다. **radar 자체가 조도 무관인 센서이므로, 야간에만 손해가 나는 것은 센서 물리가 아니라 라우팅/학습 현상이다.**

## 5. 직접 측정 수단 — 이미 배치돼 있다

**P47-2(Uni-modal Balance)의 per-modal accuracy 로그**가 이 가설의 직접 검증기다. 모달별 독립 head가 각자 GT를 얼마나 맞히는지 찍으므로, **radar의 uni-modal acc가 바닥이면 "정보 없음"이 정량 확정**된다.
- 3모달 arm(jarvis, 진행 중)에는 radar가 없으므로 확인 불가.
- **4모달 arm(hpca100, P47-1 완주 후 배치 예정)의 ep30 즉검에서 최우선 확인 항목**으로 둔다.

부가 검증(비용 낮음, GPU 여유 시): 4모달 ckpt로 **drop-radar를 day/night 분리 측정**. 가설이 맞으면 dMIoU가 **야간에서 훨씬 크게 양수**여야 한다(현 전체 +0.13은 주·야 상쇄된 값일 가능성).

## 5.5 🔴 SOTA 대조 (2026-08-04 원문 검증) — 기대치와 정답지

**① radar 증분의 정상 크기**: CAFuser Table IX(2410.10791v2, MUSES test PQ) — RGB 55.7 → +L 58.7(**+3.0**) → +R 59.3(**+0.6**) → +E 59.7(+0.4). **radar는 lidar 위에서 +0.6 PQ가 정상 상한**이다. 우리 −0.217은 "기대 미달"이 아니라 **부호가 틀린 것**이 문제다.

**② 야간 폴백의 정답지**: CAFuser Fig.5의 학습된 조건별 가중치(%)

| 조건 | RGB | Lidar | **Radar** | Events |
|---|---|---|---|---|
| Clear Day | 68 | 8 | **5** | 19 |
| Fog Night | 48 | 18 | **7** | 27 |
| 변화 | −20 | **+10** | **+2** | **+8** |

🔴 **radar는 전 조건 5~7%로 사실상 고정**이고 야간 폴백은 events·lidar가 받는다. 조건 인지 융합을 제대로 학습한 SOTA의 결론이 **"야간에도 radar를 믿지 마라"** 라는 것은, §4의 우리 가설(야간에 radar로 폴백)과 정확히 반대 방향이다.

→ **검증 추가**: 4모달 런의 router 가중치(`fusion._last_router_mean`)를 **주/야 분리 집계**해 위 표와 대조. 야간 radar 가중치가 5~7%를 크게 넘으면 기제 확정.

**③ ⚠️ 앞선 서술 완화**: §4에서 radar를 "구조적으로 정보가 낮다"고 썼으나 과했다. MUSES 원논문(2401.12761 Table 3)은 **카메라 대비 radar 단독 +4.4 PQ**(야간 +5.4)를 보고한다. 정확한 서술은 **"radar는 lidar가 있으면 대체로 잉여(CAFuser +0.6)"** 이며, 정보 자체가 없는 것은 아니다.

## 6. 함의

1. **"radar 무익" → "radar는 주간엔 소폭 유익, 야간엔 유해"로 판정을 정밀화한다.** 기존 결론(전체 −0.217)은 상쇄된 합계였다. 🔴 그리고 **"모달↑=성능↓"은 일반 법칙이 아니다** — 통제된 within-method ablation(CAFuser Table IX, DGFusion DELIVER CLE→CLDE +5.1)은 모두 단조 증가다. 리더보드의 역상관은 방법론 교란(제안서 §1.6에서 철회).
2. 처방은 **추론 시 모달 재가중이 아니다**(반증된 경로 + 조건 라벨 외부신호 금지). 학습 시 개입 = P47-2 계열이 맞다.
3. 논문 관점: "모달을 늘리면 나빠진다"는 리더보드 현상에 **조건 해상도의 증거**를 붙일 수 있다 — 단순 희석이 아니라 **약한 모달로의 폴백**이라는 구체적 기제.

관련: [[muses-dataset-setup]] · `2026-07-30-muses-drop-radar-ablation.md` · `decisions/2026-08-03-p47-mub-muses-proposal.md` · ISSUE-025
